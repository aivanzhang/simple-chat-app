def init_db(database_name="users"):
    """
    Creates database file `database_name`.csv if it exists, otherwise finish.
    @Parameter:
    1. database_name: str = file name for the database.
    @Returns: None.
    """
    with open("{}.csv".format(database_name),"w+"):
        return

def create():
    return